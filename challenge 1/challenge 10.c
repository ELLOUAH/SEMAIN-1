#include <stdio.h>
#include <stdlib.h>



int main() {
	float r,c,p;
	printf( " donner la valeur de r:");
	scanf("%f",&r);
	p=3.14;
	c=2*p*r;
	printf("la valeur de la circonference d'un cercle %f",c);
		return 0;
}
